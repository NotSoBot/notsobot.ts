"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const basecollection_1 = require("./basecollection");
;
/**
 * Members Collection
 * @category Collections
 */
class Members extends basecollection_1.BaseClientCollectionCache {
    insert(member) {
        const cache = this.insertCache(member.guildId);
        if (member.isMe) {
            cache.set(member.id, member);
        }
        else {
            if (this.enabled) {
                cache.set(member.id, member);
            }
        }
    }
    get [Symbol.toStringTag]() {
        return `Members (${this.caches.size.toLocaleString()} guilds, ${this.size.toLocaleString()} items)`;
    }
}
exports.Members = Members;
