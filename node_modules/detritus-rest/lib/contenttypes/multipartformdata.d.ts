/// <reference types="node" />
export declare class MultipartFormData {
    boundary: string;
    buffers: Array<Buffer>;
    contentType: string;
    locked: boolean;
    constructor(boundary?: string);
    add(field: string, value: any, options?: {
        contentType?: string;
        filename: string;
    } | string): void;
    done(): Buffer[];
}
