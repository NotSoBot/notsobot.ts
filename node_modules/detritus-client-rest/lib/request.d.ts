import { Request, Response } from 'detritus-rest';
import { Bucket } from './bucket';
import { Client } from './client';
export declare class RestRequest {
    readonly bucketPath?: string;
    _bucketHash?: string;
    _bucketKey?: string;
    client: Client;
    errorOnRatelimit?: boolean;
    maxRetries: number;
    request: Request;
    retries: number;
    retryDelay: number;
    skipRatelimitCheck?: boolean;
    constructor(client: Client, request: Request, options?: {
        errorOnRatelimit?: boolean;
        skipRatelimitCheck?: boolean;
    });
    readonly bucket: Bucket | null;
    readonly bucketHash: null | string;
    readonly bucketKey: null | string;
    readonly shouldRatelimitCheck: boolean;
    sendRequest(): Promise<Response>;
    send(): Promise<Response>;
}
